{
  "name": "pos-navarrete",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile start",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "db:types": "supabase gen types typescript --project-id $env:SUPABASE_PROJECT_ID > packages/db/types.ts"
  },
  "devDependencies": {
    "typescript": "^5.9",
    "@types/node": "^22"
  }
}